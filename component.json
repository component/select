{
  "name": "select",
  "repo": "component/select",
  "description": "Array filter utility",
  "version": "0.0.1",
  "keywords": ["array", "utility", "filter"],
  "scripts": ["index.js"],
  "dependencies": {
    "component/to-function": "*"
  }
}
